# Copy to .env.local and fill in with your own Supabase project's values
# (Project Settings -> API) to enable cross-device sync between caregivers.
# The app works fully offline/local without these — sync is optional.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
